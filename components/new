 <Text className="p-2">All Products</Text>
      <FlatList
        data={Products}
        numColumns={2}
        showsVerticalScrollIndicator={false}
        keyExtractor={(item) => item.id.toString()}
        renderItem={({ item }) => (
          <View className="bg-white mr-4 rounded-lg p-4 shadow-md w-40 m-4">
            <View className="items-center">
              <Image
                source={item.image}
                className="w-24 h-24"
                resizeMode="contain"
              />
            </View>
            <View className="mt-2">
              <Text className="text-sm font-semibold">{item.title}</Text>
              <View className="flex-row items-center mt-1">
                <Text className="text-lg font-bold text-green-700">
                  ${item.discount.toFixed(2)}
                </Text>
                <Text className="text-sm text-gray-500 line-through ml-2">
                  ${item.price.toFixed(2)}
                </Text>
              </View>
            </View>
          </View>
        )}
      />
    </View>